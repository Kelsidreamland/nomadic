# Phase 7 — 行李重量記錄圖表

## 目標

- 在 `Luggages` 頁面為每個行李顯示可讀的重量歷史圖表。
- 保持既有暖色系品牌風格與手機優先版面。
- 修正重量輸入流程中的無效數值風險，避免圖表讀到 `NaN`。

## 切片

### Slice 1 — 圖表元件
- 檔案：`src/components/LuggageWeightChart.tsx`
- 內容：建立可重用的重量趨勢圖元件，支援空狀態、單筆/多筆資料、最新值與變化量顯示。
- 驗證：`tsc --noEmit`

### Slice 2 — 頁面整合
- 檔案：`src/pages/Luggages.tsx`
- 內容：整合圖表、改善重量輸入狀態與提交驗證、顯示最近記錄資訊。
- 驗證：`tsc --noEmit`

### Slice 3 — 文案與預覽
- 檔案：`src/i18n.ts`
- 內容：補齊中英文圖表文案；啟動預覽後產生頁面截圖。
- 驗證：瀏覽器畫面與截圖檢查

### Slice 4 — 備份與部署
- 檔案：Git 提交與部署設定
- 內容：將本次 checkpoint 提交、推送，並嘗試部署可分享預覽。
- 驗證：`git status` 乾淨、部署命令成功
